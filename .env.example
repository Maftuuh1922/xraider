# Google OAuth Configuration (Required for deployment)
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id_here
VITE_GOOGLE_API_KEY=your_google_api_key_here

# Optional: Analytics (for production)
VITE_GA_TRACKING_ID=your_google_analytics_id

# Optional: Environment indicator
VITE_ENVIRONMENT=production

# Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Google Drive API and Google OAuth 2.0
# 4. Create OAuth 2.0 Client ID credentials
# 5. Add your production domain to authorized origins
# 6. Copy the Client ID and API Key to your .env.local file
